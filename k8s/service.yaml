apiVersion: v1
kind: Service
metadata:
  name: devops-nodejs-service
spec:
  type: NodePort
  selector:
    app: devops-nodejs-app
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30007
